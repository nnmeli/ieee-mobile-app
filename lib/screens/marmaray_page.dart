import 'package:flutter/material.dart';

class marmaray_page extends StatelessWidget {
  const marmaray_page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(alignment: Alignment.bottomCenter,child: Image.asset("lib/assets/images/animated_train.gif"),);
  }
}
